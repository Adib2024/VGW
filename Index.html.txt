<!DOCTYPE html>
<html>
  <head>
    <base target="_top">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <?!= include('CSS'); ?>
  </head>
  <body>
    <div id="refresh-indicator"><i class="fas fa-spinner fa-spin"></i> Refreshing...</div>

    <!-- Modals (Logout, Remark, CheckPart) remain identical... -->
    <!-- (I omitted modal HTML here for brevity, keep your existing modals!) -->

    <div class="app-container" id="app-container">
        <button id="global-logout-btn" onclick="requestLogout()" data-lang="btn_logout">Keluar</button>

        <div class="top-buttons-container">
          <!-- NEW: Global Home Button for Hub Navigation -->
          <button id="global-home-btn" class="btn-header-refresh" onclick="goHome()" style="display:none;">
             <i class="fas fa-home"></i>
          </button>
          
          <button id="global-refresh-btn" class="btn-header-refresh" onclick="handleRefresh()">
             <i class="fas fa-sync-alt"></i>
          </button>
          
          <div class="lang-switch">
            <select class="lang-select" onchange="changeLanguage(this.value)" id="lang-select">
              <option value="ms">Bahasa Melayu</option>
              <option value="en">English</option>
            </select>
          </div>
        </div>

      <!-- INJECT ALL VIEWS MODULARLY -->
      <?!= include('Login'); ?>
      <?!= include('Hub'); ?>
      <?!= include('Dashboard'); ?> <!-- Your Stock Take Dashboard -->
      <?!= include('Battery'); ?>
      <?!= include('QA'); ?>
      <?!= include('Details'); ?>
      <?!= include('Form'); ?>

    </div>
    <?!= include('JS'); ?>
  </body>
</html>